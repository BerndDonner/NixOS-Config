To further improve this personal lumetatex package use the following command

```sh
nix-build -A serial-studio
```

To work with the flake use

```sh
nix build .
```

instead. For debugging the build process, you can pass the --show-trace flag
to nix build for more detailed error information.
